# messaging-server

Provides a websocket endpoint clients can connect to.
Manages users, conversations and most importantly, delivery of messages between the clients.

### Build instructions

```
stack build messaging-server
stack run messaging-server-exe -- --help
```
